# Frontend Dockerfile for React development
# This image is intended for local development only. For production, build static assets
# with `npm run build` and serve via a CDN or an Nginx static container.

FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy app source
COPY . .

# Expose port 3000
EXPOSE 3000

# Start the development server
CMD ["npm", "start"]